$(document).ready(function(){

    // capter le click sur le 1er button
    $('button:first').click(function(){

        // Charger le fichier home.html dans le main
        $('main').load('views/home.html');

        

    });


});